// MOVED: the ZKEmail unseal scenario is use-case-focused code and now lives in the client SDK at
// `@nihilium/client-sdk` → `packages/client-sdk/src/scenarios/zkemail_unsealing_client.ts`.
// Core keeps only the generic scenario mechanism (`NihiliumUnsealingClient`) and the reveal-only
// `DefaultUnsealingClient`. This placeholder file is no longer imported anywhere and can be deleted.
export {};
